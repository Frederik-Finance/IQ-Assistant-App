Test it:
Make sure the email is send back properly
Establish a MongoDB connection
send questions to mongo
update user questions in mongo - done



make the logic work
store questions in mongo
and answers


Host it:



old code:

<!--
<!-- Contact Form Section -->
<section class="my-5">
    <h2 class="text-2xl mb-3">Purchase Questions</h2>
    <p>If you wish to purchase more questions, please specify the number below:</p>

    <form action="/purchase-questions" method="post" class="mt-3">
        <input type="hidden" name="userEmail" value="<%= user.email %>"> <!-- User's email from req.user -->

        <div class="mb-3">
            <label for="num-questions" class="block text-sm font-medium mb-1">Number of Questions:</label>
            <input type="number" id="num-questions" name="numQuestions" required class="p-2 border rounded-md w-full">
            <small class="text-gray-600">Hint: One question costs approximately one euro.</small>
        </div>

        <button type="submit" class="bg-blue-500 text-white p-2 rounded-md">Submit</button>
    </form>

    <% if (typeof confirmation !=='undefined' && confirmation) { %>
        <div class="mt-4 p-3 bg-green-300 text-green-800 rounded-md">
            You will be contacted shortly for your questions.
        </div>
        <% } %>
</section>
-->